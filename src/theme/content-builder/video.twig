{% if video %}
    <div class="Container Container--pad">
        <div class="my-5">
            {% if thumbnail %}
                {% do thumbnail.crop({ ar: '16x9' }) %}
            {% endif %}
            <video controls class="w-100" {{ thumbnail ? 'poster="' ~ thumbnail.src ~ '"' }}>
                <source src="{{ video.src }}" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </div>
    </div>
{% endif %}
