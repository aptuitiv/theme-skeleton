<div class="Container Container--pad">
    <div class="grid g-3">
        {% for column in columns %}
            <div class="col-1-1 col-lg-1-2 mb-2">
                {% for element in column.elements %}
                    {{ element.content }}
                {% endfor %}
            </div>
        {% endfor %}
    </div>
</div>
