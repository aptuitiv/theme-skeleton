<div class="Container Container--pad">
    <div class="grid g-3">
        {% for column in columns %}
            <div class="col-1-1 col-1-3-lg">
                {% for element in column.elements %}
                    {{ element.content }}
                {% endfor %}
            </div>
        {% endfor %}
    </div>
</div>
