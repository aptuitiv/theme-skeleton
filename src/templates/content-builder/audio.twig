{% if audioFile %}
    <audio controls preload="metadata" style="width: 100%">
        <source src="{{ audioFile.src }}">
        <p>Your browser can't display the audio player. <a href="{{ audioFile.src }}">Listen to the audio file: {{ audioFile.name }}</a>.</p>
    </audio>
{% endif %} 