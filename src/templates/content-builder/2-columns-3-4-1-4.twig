<div class="Container Container--pad">
    <div class="grid g-3">
        <div class="col-1-1 col-3-4-lg mb-2">
            {% for element in columns.0.elements %}
                {{ element.content }}
            {% endfor %}
        </div>
        <div class="col-1-1 col-1-4-lg mb-2">
            {% for element in columns.1.elements %}
                {{ element.content }}
            {% endfor %}
        </div>
    </div>
</div>
