{% if image and content %}
    <div class="Container Container--pad">
        <div class="grid">
            <div class="col-1-1 col-1-2-lg {{ imagePosition == "Right" ? 'order-2-lg' }}">
                {% do image.resize(800) %}
                {% do image.lazy() %}
                {% set image.class = "w-100" %}
                <div class="flex align-center h-100">
                    {{ image.tag }}
                </div>
            </div>
            <div class="col-1-1 col-1-2-lg {{ imagePosition == "Right" ? 'order-1-lg' }}">
                <div class="p-2 p-5-lg flex align-center h-100">
                    <div class="clearfix first-child-st-0 last-child-sb-0">{{ content }}</div>
                </div>
            </div>
        </div>
    </div>
{% endif %}
